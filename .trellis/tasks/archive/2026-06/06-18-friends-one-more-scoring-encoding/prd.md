# Run one more Friends scoring episode

## Goal

Add one more fMRI-TR-aligned Friends episode to the existing training set by:

1. Running scoring for exactly one additional episode.
2. Adding that episode to the current training split.
3. Re-running encoding with the same validation and test design as the previous finalized run.

## Selected candidate

Preferred candidate: `s01e16a`

Reasoning:

- It is an unscored S1 episode.
- Its refined description row count matches the H5 fMRI TR count exactly.
- It continues naturally after the most recent added training episode, `s01e15b`.

Known alignment:

- episode: `s01e16a`
- H5 dataset: `ses-007_task-s01e16a`
- H5 TR count: `483`
- refined description rows: `483`
- current scoring rows: `0`

## Baseline run to extend

Previous finalized encoding config:

- `configs/friends_s1s2_s06_plus4_encoding_20260617.json`

Previous finalized split:

- train: `23` episodes
- validation: `s01e02a`
- test: `s06e01a`, `s06e01b`, `s06e03a`, `s06e03b`

Previous finalized metric:

- mean subject mean test Pearson: `0.1851977718935396`

## Proposed split after this task

- train: previous `23` train episodes plus `s01e16a`, total `24`
- validation: keep `s01e02a`
- test: keep the same four S06 episodes:
  - `s06e01a`
  - `s06e01b`
  - `s06e03a`
  - `s06e03b`

## Execution plan

1. Create a dedicated one-episode scoring config for `s01e16a`.
2. Run scoring with concurrency `6`, unless zero-filled batches appear large enough to justify lowering concurrency.
3. Verify the generated score rows for `s01e16a`:
   - row count matches `483`
   - zero-filled count is not unexpectedly large
   - output path lands under the existing Friends scoring output root
4. Create a new encoding config extending `configs/friends_s1s2_s06_plus4_encoding_20260617.json`.
5. Run encoding with the updated train split and unchanged val/test split.
6. Compare test performance against the previous finalized metric.
7. Record outputs in Trellis and journal if the run completes.

## Expected new or modified files

Potential new files:

- `configs/friends_s1_1_scoring_20260618.json`
- `configs/friends_s1s2_s06_plus5_encoding_20260618.json`

Potential generated outputs:

- scoring outputs for `s01e16a`
- encoding outputs under the configured output root
- run manifests / summaries generated by the pipeline

Trellis bookkeeping:

- `.trellis/tasks/06-18-friends-one-more-scoring-encoding/prd.md`
- task context logs and final journal entry

## Risks and notes

- Scoring may produce many zero-filled rows if upstream API responses fail or rate-limit; monitor logs and reduce concurrency if this happens.
- The scoring runner may require a complete split structure. If it cannot run a one-episode config directly, use the narrowest supported runner path so only `s01e16a` is newly scored.
- Keep validation and test fixed so the new result is comparable with the previous finalized run.
- Avoid overwriting previous configs or generated metrics; write date-stamped config names.

## Execution result

- Scoring for `s01e16a` completed for all 14 ROIs.
- Each ROI wrote `279` segment score rows and `483` TR feature rows.
- Scoring warnings for `s01e16a`: `0`.
- The scoring wrapper exited non-zero only because the required `s01e02a` placeholder jobs hit existing checkpoint signature mismatches; target `s01e16a` outputs completed successfully.
- Encoding completed with manifest split `24 train / 1 val / 4 test`.
- Test episodes remained `s06e01a`, `s06e01b`, `s06e03a`, and `s06e03b`.
- Plus5 `mean_subject_mean_test_pearson`: `0.18573044881114545`.
- Previous plus4 `mean_subject_mean_test_pearson`: `0.1851977718935396`.
- Delta vs plus4: `+0.00053267691760585`.
